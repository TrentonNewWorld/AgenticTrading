[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "newworldtrading"
dynamic = ["version"]
description = "Lightweight Python client for the NewWorldTrading REST API (LLM-powered trading agents, backtests, paper trading)."
readme = "README.md"
requires-python = ">=3.9"
license = { file = "LICENSE" }
authors = [
    { name = "NewWorldTrading" },
]
keywords = [
    "trading",
    "agents",
    "llm",
    "backtesting",
    "finance",
    "quant",
    "paper-trading",
    "agentic",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Financial and Insurance Industry",
    "Intended Audience :: Science/Research",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Office/Business :: Financial :: Investment",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
# Windows ships no IANA time-zone database, so `zoneinfo` cannot resolve
# "America/New_York" there (used by the TradingAgents replay planner). This is
# the only dependency, and it installs on Windows alone: POSIX platforms keep a
# zero-dependency install.
dependencies = ["tzdata; platform_system == 'Windows'"]

[project.optional-dependencies]
vnpy = [
    "vnpy==4.4.0",
    "vnpy_ctastrategy==1.3.0",
]

[project.urls]
Source = "https://github.com/TrentonNewWorld/NewWorldTrading"
Issues = "https://github.com/TrentonNewWorld/NewWorldTrading/issues"

[project.scripts]
newworldtrading = "newworldtrading.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.dynamic]
version = { attr = "newworldtrading.__version__" }

[tool.pytest.ini_options]
pythonpath = ["src"]
